# React and Tailwind CSS Starter

## Getting started

Go into the project and install the packages with the following command:
```
yarn install
```
After that to start the app just use:
```
yarn start
```
